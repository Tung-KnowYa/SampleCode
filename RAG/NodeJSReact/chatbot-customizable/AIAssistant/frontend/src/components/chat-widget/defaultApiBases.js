/** Default origins when `api-base` is omitted on the custom elements. Trimmed strings. */

/** Full chat widget (`ai-chatbot-widget`): empty ⇒ same-origin `/api/chat`. */
export const DEFAULT_API_BASE_AI_CHATBOT = '';

/**
 * Knowledge-only widget (`knowledge-chatbot-widget`): change if this embed should target another origin by default.
 */
export const DEFAULT_API_BASE_KNOWLEDGE_CHATBOT = 'http://localhost:3002';
